// Database seeding is now handled by backend scripts
// Use: cd backend && node scripts/seedServices.js
export const seedDatabase = async () => {
  console.log('Database seeding is now handled by backend scripts.');
  console.log('To seed the database, run: cd backend && node scripts/seedServices.js');
};
